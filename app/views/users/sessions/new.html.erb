<div class="auth-wrapper">
  <div class="auth-card">
    <h2 class="auth-title">ログイン</h2>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field">
        <%= f.label :email, "メールアドレス", class: "form-label" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "example@email.com", class: "form-input" %>
      </div>

      <div class="field">
        <%= f.label :password, "パスワード", class: "form-label" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "form-input" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field remember-me-field">
          <%= f.check_box :remember_me, class: "checkbox-input" %>
          <%= f.label :remember_me, "ログイン状態を保存する", class: "checkbox-label" %>
        </div>
      <% end %>

      <div class="actions">
        <%= f.submit "ログイン", class: "btn-submit" %>
      </div>
    <% end %>

    <div class="auth-links">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>